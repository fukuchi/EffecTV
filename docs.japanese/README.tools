本筋とは関係のない小さなツール群です。

AddEffect.pl
	新しいイフェクトを書き始めるときに使うPerlスクリプト。
	雛型を自動生成します。

palettecheck
	使っているv4lドライバがサポートしているピクセルフォーマットを列挙します。
	EffecTVは全てのフォーマットをサポートしている訳ではないので、使っている
	機器によってはEffecTVが動作しない場合があります。もしお使いの機器で動か
	ない場合ば、この'palettecheck'の出力をバグレポートメールにくっつけていた
	だけると助かります。コンパイルと起動には
	$ make palettecheck
	$ ./palettecheck
	としてください。
